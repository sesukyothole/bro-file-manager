services:
  bro-file-manager:
    build: .
    ports:
      - "3033:3033"
    environment:
      PORT: 3033
      FILE_ROOT: /data
      ADMIN_PASSWORD: change-me
      SESSION_SECRET: change-me
      AUDIT_LOG_PATH: /app/logs/audit.log
    # Optionally load environment variables from .env (Compose-level).
    # env_file:
    #   - .env
    volumes:
      - ./data:/data
      - ./logs:/app/logs
