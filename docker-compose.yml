services:
  bro-file-manager:
    build: .
    ports:
      - "3033:3033"
    environment:
      PORT: 3033
      FILE_ROOT: /data
      ADMIN_PASSWORD: change-me
      SESSION_SECRET: change-me
      AUDIT_LOG_PATH: /app/logs/audit.log
    volumes:
      - ./data:/data
      - ./logs:/app/logs
